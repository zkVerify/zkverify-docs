---
title: 状态与证明存储
---

# zkVerify Mainchain State and Storage

区块存储策略取决于节点类型。

## 验证人节点
验证人节点默认配置为 archive 节点，存储从创世到最新的完整状态及所有区块（包含其中的证明）。可改为开启修剪，仅保留最近 N 个区块，但对验证人节点而言被视为不安全。

## 普通节点
普通节点默认启用修剪，仅保留最近 256 个区块，可显著节省磁盘。也可改为 archive 模式，与验证人节点一致。
